# 服务配置
LSA_HOST=127.0.0.1
LSA_PORT=8008

# OpenAI-compatible Chat Completions 配置
# 不配置 LSA_API_KEY 时，系统仍可检索文档，但只返回相关片段，不调用大模型生成最终答案。
LSA_API_KEY=你的API_KEY
LSA_BASE_URL=https://api.openai.com/v1
LSA_MODEL=gpt-4o-mini

# 发送给大模型的最大上下文字数
LSA_MAX_CONTEXT_CHARS=6000
