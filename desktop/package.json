{
  "name": "hoya-agent-desktop",
  "version": "1.2.2",
  "description": "Local AI workspace agent for Windows",
  "author": "lihongyao517",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lihongyao517/Hoya_agent.git"
  },
  "private": true,
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "build:backend": "powershell -NoProfile -ExecutionPolicy Bypass -File ../build_backend.ps1",
    "build:renderer": "vue-tsc --noEmit && vite build",
    "test:electron": "node --test tests/*.test.cjs",
    "test": "npm run test:electron",
    "build": "npm run build:renderer",
    "pack": "npm run build:renderer && electron-builder --dir",
    "clean:old-artifacts": "node scripts/clean-old-artifacts.cjs",
    "dist": "npm run build:renderer && electron-builder && npm run clean:old-artifacts",
    "dist:full": "npm run build:backend && npm run dist",
    "release:prepare": "npm run dist:full && npm run verify:update-artifacts",
    "verify:update-artifacts": "node scripts/validate-update-artifacts.cjs",
    "start": "electron ."
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@element-plus/icons-vue": "^2.3.2",
    "@vitejs/plugin-vue": "^6.0.8",
    "concurrently": "^9.0.0",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "element-plus": "^2.14.3",
    "typescript": "^5.0.0",
    "vite": "^7.0.0",
    "vue": "^3.5.40",
    "vue-tsc": "^3.3.7",
    "wait-on": "^8.0.0",
    "yaml": "^2.9.0"
  },
  "build": {
    "appId": "com.hoya.agent.desktop",
    "productName": "Hoya Agent",
    "copyright": "Copyright (c) 2026 lihongyao517",
    "directories": {
      "output": "../artifacts/desktop"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "assets/icon.ico",
      "assets/icon.png",
      "assets/icon.svg",
      "package.json"
    ],
    "electronLanguages": [
      "en-US",
      "zh-CN"
    ],
    "extraResources": [
      {
        "from": "../artifacts/backend",
        "to": "backend",
        "filter": [
          "**/*"
        ]
      }
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "lihongyao517",
        "repo": "Hoya_agent",
        "releaseType": "release"
      }
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "assets/icon.ico"
    },
    "icon": "assets/icon.png",
    "nsis": {
      "artifactName": "Hoya-Agent-Setup-${version}-${arch}.${ext}",
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": "always",
      "createStartMenuShortcut": true,
      "shortcutName": "Hoya Agent"
    },
    "portable": {
      "artifactName": "Hoya-Agent-Portable-${version}-${arch}.${ext}"
    }
  }
}
